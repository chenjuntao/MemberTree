<Window x:Class="MemberTree.SearchMultiInput"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	Title="查找导出多个" Height="500" Width="250" WindowStartupLocation="CenterScreen">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition />
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		<GroupBox Header="查找结果要保存的位置">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition />
					<ColumnDefinition Width="30"/>
				</Grid.ColumnDefinitions>
				<TextBox Name="txtPath" IsReadOnly="True" TextWrapping="Wrap" />
				<Button Content="浏览" Grid.Column="1" Click="Browse_Click"/>
			</Grid>
		</GroupBox>
		<ComboBox Name="comboSearchType" Grid.Row="1">
            <ComboBoxItem IsSelected="True">根据ID查找</ComboBoxItem>
            <ComboBoxItem>根据姓名查找</ComboBoxItem>
            <ComboBoxItem>根据级别查找</ComboBoxItem>
            <ComboBoxItem>根据下线人数查找</ComboBoxItem>
    		<ComboBoxItem>根据下线层数查找</ComboBoxItem>
        </ComboBox>
		<GroupBox Name="groupUsr" Header="输入多个查询关键字，每个关键字一行" Grid.Row="2">
			<TextBox Name="txtUsr" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Visible"/>
		</GroupBox>
		<ScrollViewer Name="scrollUsr" Grid.Row="2" Visibility="Hidden">
			<StackPanel Name="listUsr"/>
		</ScrollViewer>
		<Button Name="btnOK" Content="开始查找" Grid.Row="3" IsEnabled="False" Click="Button_Click"/>		
	</Grid>
</Window>